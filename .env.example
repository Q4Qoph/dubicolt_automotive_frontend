NEXT_PUBLIC_USE_API=true
# Optional. Leave unset in dev to use Next proxy at /api → http://localhost:3001
# NEXT_PUBLIC_API_BASE_URL=http://localhost:3001/api

# Used by next.config.js rewrites (server-side proxy target)
# API_PROXY_TARGET=http://localhost:3001
